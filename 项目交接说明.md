# 财升宝 APP 智能化升级 Demo 项目交接说明

## 项目定位

该项目是面向客户演示的交互原型，使用标准 Next.js App Router 构建，可直接部署到 Vercel。项目不依赖数据库、登录系统或第三方 API。

## 环境与命令

- Node.js：`>=20.9.0`，推荐 Node.js 22。
- 安装依赖：`npm ci`
- 本地开发：`npm run dev`
- 代码检查：`npm run lint`
- 生产构建与测试：`npm test`
- 本地生产运行：`npm run build && npm run start`

## Vercel 发布

将项目提交到 Git 仓库后，在 Vercel 导入即可。Vercel 应识别为 `Next.js`，无需配置环境变量和输出目录。

如果仓库中还包含其他项目，导入时将 Root Directory 指向本项目目录：

```text
[Prototype] caishengbao-ai-demo-20260723-171211
```

## 主要文件

- `app/page.tsx`：核心页面和交互。
- `app/globals.css`：全局样式。
- `app/api/recommendations/route.ts`：Banner 推荐演示接口。
- `public/`：图片资源。
- `tests/rendered-html.test.mjs`：关键功能回归检查。
- `README.md`：开发与部署说明。
- `vercel.json`：Vercel 框架声明。

## 演示数据

所有行情、公告、研报和 AI 结果均为原型示例。自选数据存储在浏览器本地，同一浏览器刷新后可保留；更换设备或清理浏览器数据后会恢复默认自选列表。
